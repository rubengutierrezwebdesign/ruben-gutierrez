<!DOCTYPE html>
<html lang="en">
    {%- include 'head.njk' -%}
<body>

    {%- include 'nav.njk' -%}

    <div class="mac">
        <img style="margin:0 auto;" src="/assets\images\jekyll-to-11ty.png" alt="">
    </div>

    <!-- Container -->
    <div class="mac mtb-50 flex" style="max-width:900px;">
        
        <!-- Post Container -->
        <div class="inner-container post border-box paa-25">
            
            <!-- Post Information Container -->
            <div class="inner-container mb-50">    

                <!-- Title -->
                <h1 style="text-align:center;">{{title}}</h1>

                 <!-- Date -->
                <p class="_10 _600">{{ page.date | postDate }}</p>

            </div>

            <!-- Content Container -->
            <div class="mtb-50"style="max-width: 760px;">
                <!-- Content -->
                {{ content  | safe}}
            </div>
      
        </div>
            
    </div>

    {# Related Posts #}
        <div class="mac paa-25  mt-50">
            
            {# Recent Posts #}
            <div class="recent ">
                <h2 class="h3 _600">Recent Posts</h2>

                {% for post in collections.posts | reverse %}
                    {% if loop.index0 < 3 %}

                            <article class=" mb-25" style="background:white;">

                                <div class="" style="">
                                    <a href="{{post.url}}" style="text-decoration:none;"><h3 class="h5 _800">{{post.data.title }}</h3></a>
                                    <p class="_12">{{post.data.description | truncate(100)}}</p>   
                                </div>

                            </article>

                        {% endif %}
                    {% endfor %}
                </div>
        </div>
        
        
        {%- include 'footer.njk' -%}


</body>
</html>