---
layout: 'default.njk'
title:  "Los Angeles"
permalink: /los-angles/
tags: local
---

<div class="paa-20">
     <div class="cont">

        <h1>Website Design Services in Los Angeles, California</h1>
        {% for post in collections.los_angeles | reverse %}
            {% if loop.index0 < 4 %}
                <div>
                    <a href="{{post.url}}"><h2>{{post.data.display_title}}</h2></a>
                    <p>{{post.data.description}}</p>   
                </div>
            {% endif %}
        {% endfor %}
    </div>
</div>