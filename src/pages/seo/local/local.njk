---
layout: 'default.njk'
title:  "Local"
permalink: /local/
---
<div class="cont mb-100">

    <div>
        <h1>Local SEO Directory</h1>
        <p>Directory for local pages in North Orange County.</p>
    </div>

    <div class="flex flex-wrap">
        {% for post in collections.local | reverse %}
            {% if loop.index0 < 10 %}
                <div class="br paa-20 mb-20" style="width:200px;">
                    <a href="{{post.url}}" style="text-decoration:none"><h3 class="_18">{{post.data.title}}</h3></a>
                </div>
            {% endif %}
        {% endfor %}
    </div>
</div>