---
title:     Blog
layout:    'default.njk'
permalink: /web-dev/
---

<div class="paa-20">

    <div class="cont pd-50">
        <h1>Website Development</h1>
        <p>Are you intrested in learning about website development and design.Tutorials about Website Developments, Search Engine Optimization, Website Design and More.</p>
    </div>

    
    <div class="cont pd-50">
        <h2>Websites</h2>
        {% for post in collections.websites | reverse %}
            {% if loop.index0 < 5 %}
                <a href="{{post.url}}"><h3>{{post.data.title}}</h3></a>
            {% endif %}
        {% endfor %}
    </div>

    
    <div class="cont pd-50">
        <h2>Search Engine Optimization</h2>
        {% for post in collections.seodev | reverse %}
            {% if loop.index0 < 5 %}
                <a href="{{post.url}}"><h3>{{post.data.title}}</h3></a>
            {% endif %}
        {% endfor %}
    </div>

<div